# 1PoundBid Environment Configuration

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url                  # Required: Supabase project URL
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key       # Required: Supabase anonymous key

# Development Configuration
NODE_ENV=development                                 # Optional: Environment mode (development/production)
VITE_DEV_MODE=true                                  # Optional: Enable development features

# Notes:
# - All VITE_ prefixed variables are exposed to the client
# - Store sensitive credentials only in server-side environment variables
# - Keep this file as .env.example in version control
# - Create a local .env file with actual values (do not commit)
# - Required variables must be set for the application to function